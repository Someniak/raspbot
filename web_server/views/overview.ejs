<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="icon" type="image/png" href="/public/favicon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Raspbot</title>

    <!-- Bootstrap -->
    <link href="/dist/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="/dist/html5shiv.min.js"></script>
    <script src="/dist/respond.min.js"></script>
    <![endif]-->
    <script src="/dist/socket.io-1.4.5.js"></script>
    <script src="/dist/jquery-2.2.4.min.js"></script>
    <script src="/dist/jquery.terminal.min.js"></script>
    <script src="/js/overview-script.js"></script>
    <link href="/dist//jquery.terminal.min.css" rel="stylesheet"/>

    <link href="/css/style.css" rel="stylesheet" />
</head>
<body>
<div class="container">

    <h1>OVERVIEW</h1>
    <form action="/logout" method="post" class="pull-right">
        <input type="submit" value="LOGOUT" class="btn btn-danger"/>
    </form>
    <h2>CONTROL</h2>
    <div id="terminal"></div>
    <h2>Current connections (<span id="number_of_connections"><%= connections.length%></span>)</h2>
    <table class="table table-striped table-hover table-responsive table-condensed" id="connections">
        <thead>
        <tr>
            <th>ID</th>
            <th>NAME</th>
            <th>IP</th>
            <th>PORT</th>
            <th>ACTION</th>
        </tr>
        </thead>
        <tbody>
        <% for(var connection of connections){ %>
        <tr data-connection-id="<%= connection.id %>">
            <td><%= connection.id %></td>
            <td><%= connection.name %></td>
            <td><%= connection.ip %></td>
            <td><%= connection.port %></td>
            <td><a href="/control/<%= connection.id %>" class="btn btn-success btn-xs">CONTROL</a></td>
        </tr>
        <%}%>
        </tbody>
    </table>
</div>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="/dist/bootstrap.min.js"></script>
</body>
</html>
